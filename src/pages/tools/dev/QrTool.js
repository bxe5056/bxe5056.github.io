import React from "react";

/** Placeholder — feature agent replaces this body (QR generate + scan). */
const QrTool = () => (
  <div className="py-12 text-center text-gray-500">
    <p className="text-lg font-medium text-gray-700">QR Code</p>
    <p className="mt-2 text-sm">Coming soon</p>
  </div>
);

export default QrTool;
